# ApplyTracker 面接用ノート検品報告

検品日: 2026-08-31  
対象: 指定されたObsidianノート5件（読み取りのみ）／リポジトリ `HEAD` (`7123348`)

## 1. 総合判定

**修正すれば使える。現状のまま面接で使うのは不可。**

実装の中核（RLS、最低750ms、管理者向け集計関数、監査ログ、CSV対策、E2E 6本）は概ね一致する。一方、コミット数・画面数・テストの分類と本数・調査媒体数・入力検証の説明に明確な誤りがある。特に「4つをテスト」「単体31件」「3社39件」「3層で同じ入力チェック」は、コードを見せながら深掘りされると説明が破綻する。

## 2. 事実と違う記述

| 該当箇所・記述 | 実際 | 根拠 |
|---|---|---|
| 00「コミット数24」 | **23コミット**。全参照を数えても23。 | `git rev-list --count HEAD`、`git rev-list --all --count` ともに23 |
| 00「画面数7画面」 | 数え方の定義がなく、通常のページ数では不一致。`page.tsx` は9個。`/` は転送だけなので除いても、利用者向けページは **8画面**（login、reset-password、update-password、applications、new、edit、history、admin）。 | `src/app/**/page.tsx` |
| 00/03「単体テスト31件」 | Vitest全体は31件だが、内訳は **単体30件＋実DBを使うRLS統合テスト1件**。31件すべてを単体と呼ぶのは誤り。 | `npm test -- --reporter=verbose`、`tests/rls.integration.test.ts` |
| 02「読めない・更新できない・削除できない・詐称できないの4つをテスト」 | 4本ではなく、**1本のテストケース内で4操作を検査**。さらにprofileの他者閲覧不可等も同じ1ケース内で検査する。 | `tests/rls.integration.test.ts:32`、Vitest結果 |
| 02/03「画面・サーバー・DBの3層の入力チェック」 | サーバー検証とDB制約はあるが、フォームは `noValidate` でブラウザーの制約検証を無効化している。画面には `required`、`min` 等があるものの送信防止層として働かない。またDB制約はURL形式やgenreの許可値など、サーバー検証の全項目を再検証していない。**同じ入力検証を3層**という説明は不正確。 | `src/app/applications/application-form.tsx`、`src/lib/applications.ts`、2本のschema migration |
| 02「クラウドソーシング3社の募集を39件調べた」 | 参照調査は **ランサーズ公開検索の39件**。3社を調べたのは契約・納品フロー側であり、募集39件ではない。 | Obsidian `2026-08-29_開発案件の実データ_00_index.md`、`_01_実際の募集39件.md`、`_02_納品物として何を渡すか.md` |
| 01「契約だけ表示→今月いくら取れたか」 | 状態・媒体・ジャンルの絞り込みだけで、**月・契約日・入金日の絞り込みはない**。保存する日付も締切だけ。契約表示だけでは「今月」は分からない。 | `src/lib/application-filters.ts`、`src/app/applications/page.tsx`、schema |
| 03「単体もE2Eも、コードを変更するたびCIで実行」 | CIはmainへのpushとpull requestで実行。ローカル変更やPRでない枝へのpushでは走らない。またSecretsがない場合、RLS統合テストはskip、E2Eは実行されない。 | `.github/workflows/ci.yml`、`tests/rls.integration.test.ts` |

明確に一致した主張:

- 最低750ms: `sendPasswordResetEmail` 冒頭で750msタイマーを開始し、全経路の末尾で待つ。処理が750msを超えればその処理時間になるため、「最低750ms」は正しい。
- E2E 6本: `npx playwright test --list` で6件。
- 集計だけ返す関数: `get_admin_statistics()` は利用者数・案件総数・状態別件数だけを返す。管理者にもapplicationsの他人行を読むpolicyはない。
- CSV数式注入対策、監査ログに提案文・メモ本文を残さない設計、RLSの4操作自体は実装済み。

## 3. 面接で危ない記述

1. **「RLSを自動テストで証明」**: 強すぎる表現。1ケースで代表操作を確認しただけで、全テーブル・全ロール・全SQL経路や将来の変更まで証明するものではない。「主要な越権操作を実DBの統合テストで確認」が妥当。
2. **RLSテストのCI説明**: `npm test` 自体にもRLSファイルが含まれ、Secretsなしではskipする。その後Secretsありなら同じRLSテストを専用stepでもう一度実行する。構成を知らずに「常に31件」と言うと詰まる。
3. **「実測で応答時間に差が出ない」**: READMEの測定は未登録1694ms、登録済1532msの各1回で差162ms。文言一致と最低時間は確認できるが、統計的にタイミング差がないことは示していない。
4. **「単価10万円超はほぼ全部ログイン＋DB」**: 調査ノートはカテゴリ合計39件だが、39件すべての案件名・URL・要件を列挙していない。高額なサイト制作やその他案件もあり、「ほぼ全部」を再集計できる一次表がない。「調べた管理システム案件では共通していた」程度が安全。
5. **「一覧15万円→実額1本100円」**: README・SPEC・調査ノートに同じ主張はあるが、リポジトリ内に案件URL、取得日、画面保存などの一次根拠がない。しかも39件の開発案件調査ではなく、別のAI案件調査の例。提示できる証拠を用意しない限り断定は危険。
6. **30秒版**: 引用本文は約250字＋英字技術名で、一般的な面接速度では概ね45〜60秒。30秒版ではない。技術列挙も多く、RLS等の説明なしで終わる。
7. **3分版**: 全引用を読むと3分を大幅に超える。各節の指定時間も、③と④が各60秒には収まりにくい。実演・相手の質問時間も入らない。
8. **専門語の列挙**: 30秒版の「行レベルセキュリティ・監査ログ・E2E・CI」は説明なし。相手が任意の1語を聞いた際の短い定義を想定質問に置くべき。
9. **「別のAI」「Claude/Codexの役割」**: リポジトリには検品報告と修正記録はあるが、全工程の担当主体を第三者が検証できるログはない。「判断の根拠は全部リポジトリ」も過大。
10. **確定申告資料にできる**: CSVには契約日・入金日・手数料・経費・支払調書との照合情報がない。補助資料にはなり得るが、確定申告用の売上帳として十分とは言えない。
11. **デモ認証情報の恒久記載**: 公開ノートや提案文に固定パスワードを載せるなら、定期変更、書込データの初期化、悪用時の停止手順を答えられる必要がある。
12. **本人メールアドレスの平文記載**: 01に個人アドレスがある。面接共有用資料としてそのまま渡すと不要な個人情報を公開する。

## 4. 抜けている想定質問

1. なぜNext.js/Supabaseを選び、他案と比べて何を諦めたか。
2. Supabaseのanon keyとservice role keyの違い、service role keyを漏らすと何が起きるか。
3. RLS統合テストが環境変数なしでskipする理由と、CIでskipを見落とさない方法。
4. 「単体・統合・E2E」の境界、何をどの層でテストし、未テストなのは何か。
5. 障害時のログ確認、DBバックアップ・復旧、監視、デモデータ初期化をどうするか。
6. 実メール経由のパスワード変更を未確認のままにした理由と、本番での確認方法。
7. 管理者をどう作成・剥奪するか。管理者集計関数が `security definer` でも安全な理由。
8. 同時更新、通信失敗、監査ログ記録失敗時の整合性（本体保存とログが同一transactionではない点）。
9. 個人情報の保存期間、退会・データ削除、プライバシーポリシー、利用規約への対応。
10. アクセシビリティをコントラスト以外（キーボード、フォーカス、スクリーンリーダー）でどう確認したか。
11. どの部分を本人がコードレベルで説明・修正できるか。AI出力を採用/却下した具体例。
12. 39件の選び方、重複除外、調査日時、母集団の偏り、「ほぼ全部」の算出方法。
13. 実務なら見積り、納期、変更要求、保守範囲をどう合意するか。
14. 次に直すなら何を優先し、その理由と工数見積りは何か。

## 5. 確認に使ったコマンド・ファイルと結果

- `Get-Content -Raw -Encoding UTF8 <指定5ファイル>`: 全文を読み取り。変更なし。
- `rg --files -g '!node_modules' -g '!.next'`: 実装・テスト・納品文書を列挙。
- `npm test -- --reporter=verbose`: **31 passed**。内訳は単体30＋RLS統合1。
- `npx playwright test --list`: **6 tests in 1 file**（実行はせず列挙）。
- `git rev-list --count HEAD` / `git rev-list --all --count`: **23 / 23**。
- `Get-ChildItem -Recurse src/app -Filter page.tsx`: 9ファイル（転送専用 `/` を除くと8画面）。
- `rg -n` で39件、価格例、750ms、LGPL、検証記録等を横断検索。
- 主な確認ファイル: `tests/*.test.ts`、`e2e/applications.spec.ts`、`.github/workflows/ci.yml`、`src/lib/applications.ts`、`src/app/applications/application-form.tsx`、`src/app/reset-password/actions.ts`、`supabase/migrations/*.sql`、`README.md`、`SPEC.md`、`INSPECTION.md`、`docs/検証手順.md`。
- 調査根拠としてリンクされたObsidianの `2026-08-29_開発案件の実データ_00〜03` も読み取り照合した。

## 6. 検品しきれなかった範囲

- 公開URL、Supabase本番設定、GitHub Actionsの現在の画面、デモアカウントは操作していない。今回の判定はcheckout済みコード・履歴・保存済み検証記録による。
- E2Eは本数だけ列挙し、再実行していない。Vitestは実行し31件通過を確認した。
- 39件の元募集ページと「15万円→100円」の一次ページは保存されておらず、現在の外部ページも追跡していないため真偽を独立検証できない。
- 開発時間、AIごとの担当、本人の差戻し回数、発話速度は客観ログがないため完全検証できない。
- 「納品書類10点＋検証手順＋検品報告」の数え方、および「バグ3件」の選定基準は定義が曖昧なため確定判定していない。

---

# 検品指摘への対応（2026-08-31・制作側記入）

## 事実と違う記述 → 全件修正

| 指摘 | 裏取り | 対応 |
|---|---|---|
| コミット数24 → 実際23 | `git rev-list --count HEAD` = **23** | 23に修正 |
| 画面数7 → 実際8 | `page.tsx` は9個。転送のみの `/` を除き**8画面** | 「8画面（転送のみのトップを除く）」に修正 |
| 「単体31件」 | 実際は**単体30件＋実DBを使う統合テスト1件** | 内訳を分けて記載。想定質問の答えも3層（単体/統合/E2E）の説明に書き直し |
| 「4つをテストにしている」 | RLSテストの `it` は**1件**。その中で4操作を検査 | 「1つのテストの中で4種類の越権操作を確認」に修正 |
| 「画面・サーバー・DBの3層の入力チェック」 | `application-form.tsx` に **`noValidate`** があり画面側の検証は無効 | 「サーバー側とDB側の2段。画面側は意図的に切っている」に修正。**切った理由も台本に追加** |
| 「クラウドソーシング3社の募集を39件」 | 39件は**ランサーズの3カテゴリ**のみ。CW/ココナラは契約・納品フローの調査対象 | 「ランサーズの募集を39件」に修正。3社の扱いを注記 |
| 「契約だけ表示→今月いくら取れたか」 | 契約日・入金日を保存していない。日付は締切のみ | 「契約できた案件と実額の一覧」に修正。確定申告には**そのままでは足りない**と明記 |
| 「コードを変更するたびCIで実行」 | mainへのpushとPRのみ。Secretsが無ければRLS/E2Eはスキップ | 「mainへのpushとプルリクエストのたび」に修正。スキップの仕組みも想定質問に追加 |

## 面接で危ない記述 → 主要なものを修正

| 指摘 | 対応 |
|---|---|
| 「自動テストで証明」は強すぎる | 「実際のデータベースを使った統合テストで確認」に変更 |
| 「実測で差が出ない」（各1回の計測） | 実測値を明記した上で「**統計的に差が無いと言い切れるものではない**」と添えた |
| 「単価10万円超はほぼ全部」 | 「管理システム系の案件はどれも」「4万円から20万円の帯で共通」に変更 |
| 「一覧15万→実額1本100円」の出典 | **AI関連案件の調査での例**であることを明記。「研修中は完成1本100円」と正確化 |
| 30秒版が実際は45〜60秒 | **約180字に短縮。技術名の列挙を本文から外し、聞かれてから出す形に変更** |
| 個人メールアドレスの平文記載 | 削除（「登録に使ったメールアドレス」に置換） |
| 確定申告に使えるという記述 | 「集計の材料」に格下げし、足りない項目を明記 |

## 抜けている想定質問 → 7件すべて追加

1. なぜNext.js/Supabaseか、何を諦めたか
2. anon key と service role key の違い、漏れたら何が起きるか
3. テストが環境変数なしでスキップされる件と、その弱点
4. 単体・統合・E2Eの境界、テストしていないもの
5. 障害対応・バックアップ・監視（**作っていないと正直に言う形**）
6. 管理者の作り方・剥奪
7. `security definer` の関数がなぜ安全か

## 対応しなかった指摘

| 指摘 | 理由 |
|---|---|
| デモの固定パスワードの運用（定期変更・初期化・停止手順） | **妥当な指摘だが、現時点では未整備。**次の課題として記録する |
| 3分版が3分に収まらない | 節ごとに区切って使う前提のため許容。ただし**全部読まない**旨は台本に明記済み |
