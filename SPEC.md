# 案件応募トラッカー — 仕様書

- 作成日: 2026-08-29
- 作る人: 竹元洋実
- 分担: Claude=仕様書と検証 / Codex=実装 / 竹元さん=最終判断

---

## 0. なぜこれを作るのか（1行で）

**実案件で単価10万円を超える層はほぼ全部「ログイン＋サーバー側DB」が前提だが、
今までの3作品にそれが1本もない。その穴を1本で埋める。**

題材を「案件応募トラッカー」にした理由:
- クラウドソーシングに登録した直後で、**自分が確実に毎日使う**
- 面接で「仕事を取るために自分で作った」と語れる
- 記録する中身（案件・報酬・応募数・結果）が、そのまま面接の話題になる

技術構成の根拠: ココナラの実案件「キャッシュフロー健康診断Webアプリ(MVP)・60〜90万円」の
募集要件が **Next.js / Node API / PostgreSQL・Supabase / Vercel** そのもの。
（[調査メモ](../../OneDrive/ドキュメント/Obsidian%20Vault/第2の脳/リサーチ/2026-08-29_開発案件の実データ_00_index.md)）

---

## 1. 完成の定義（これが全部できたら完成。ここを増やさない）

1. **第三者がREADMEだけを見て自分のPCで起動できる**
2. デモ用アカウントが用意してある（採用担当がすぐ触れる）
3. **別のユーザーのデータが絶対に見えないことを、自動テストで証明している**
4. GitHub Actions のCIが緑（PRを出すとlintとtestが自動で走る）
5. 本番URLで公開されていて、誰でも触れる
6. 納品10点セットの書類が全部そろっている（下の6章）

**機能を10個作るより、主要3フローを堅牢にする。**

---

## 2. 主要3フロー（これだけ完璧にする）

### フロー1: アカウント
登録 → ログイン → ログアウト → パスワード再設定

### フロー2: 案件の記録
案件を登録 → 編集 → 状態を変える → 削除

### フロー3: 見返す
一覧を絞り込む → CSVで書き出す

---

## 3. 画面（全4枚。増やさない）

| # | 画面 | 中身 |
|---|---|---|
| 1 | ログイン / 新規登録 | メールとパスワード。パスワード再設定への導線 |
| 2 | 案件一覧 | 表形式。状態で絞り込み。CSV出力ボタン。新規登録ボタン |
| 3 | 案件の登録・編集 | 下の項目を入力するフォーム |
| 4 | 管理者画面 | 全ユーザーの件数だけ見える（中身は見えない）。RBACの実証用 |

### 記録する項目（案件1件あたり）

| 項目 | 型 | 必須 |
|---|---|---|
| 媒体 | クラウドワークス / ランサーズ / ココナラ / その他 | ○ |
| 案件タイトル | 文字列 | ○ |
| 募集ページURL | 文字列 | |
| 一覧に出ていた金額 | 文字列（「10万〜20万円」等） | |
| **本文で確認した実額** | 数値 | |
| 応募者数 | 数値 | |
| クライアント評価 | 数値 | |
| クライアント完了率 | 数値(%) | |
| 締切 | 日付 | |
| 状態 | 検討中 / 応募済 / 返信あり / 契約 / 不成立 / 見送り | ○ |
| 提案文 | 長文 | |
| メモ（なぜ落ちたか等） | 長文 | |

※「一覧の金額」と「実額」を分けて持つのは、
**一覧表示15万円→実額1本100円だった実例**が今回の調査で出たため。

---

## 4. 技術構成（Codex案を採用）

| | 使うもの |
|---|---|
| 画面 | Next.js (App Router) + TypeScript |
| DB | PostgreSQL（Supabase） |
| 認証 | Supabase Auth（メール＋パスワード） |
| 権限 | Row Level Security ＋ ロール2種（一般 / 管理者） |
| スキーマ管理 | migration（SQLファイルで履歴を残す） |
| テスト | Vitest（ロジック）+ Playwright（画面の通し操作） |
| CI | GitHub Actions（PRでlint + test） |
| 公開 | Vercel |
| その他 | 監査ログ、CSV出力 |

**認証は「ログイン画面がある」では不十分。**
登録・パスワード再設定・セッション・権限・**他人のデータ遮断**まで作って初めて評価される。

---

## 5. 進め方（1周1〜3時間で切る。全6周）

| 周 | やること | 終わりの合図 |
|---|---|---|
| 1 | DB設計（ER図）＋ migration ＋ 認証（登録/ログイン/ログアウト） | 2人のユーザーを作り、**互いのデータが見えないことをテストで確認** |
| 2 | 案件のCRUD（一覧・登録・編集・削除） | 画面から1件登録して編集して消せる |
| 3 | 一覧の絞り込み ＋ CSV出力 | 状態で絞ってCSVが落ちる |
| 4 | パスワード再設定 ＋ 監査ログ ＋ 管理者ロール | 管理者画面が権限で守られている |
| 5 | Playwright のE2E ＋ GitHub Actions のCI | PRを出すとCIが緑になる |
| 6 | Vercelへ公開 ＋ 納品10点セットの書類 | 本番URLで他人が触れる |

**各周のあと、Claudeが実際にブラウザで動かして検証する。** 目視ではなく数値で測る。

---

## 6. 納品10点セット（6周目で全部そろえる）

実案件で求められるもの。これを作るのが今回の裏の目的。

1. Gitリポジトリ / ソース一式
2. 本番URL
3. **DBスキーマ・migration・初期データ**
4. **環境変数一覧**（`.env.example`。秘密の値そのものは書かない）
5. **README**（セットアップ / 操作 / 障害復旧）
6. **受入テスト表**（操作 / 期待結果 / 実際の結果 の3列）＋ 自動テスト結果
7. アカウント・ドメインの引き渡し手順
8. **既知の制約**（できないこと）
9. ライセンス一覧
10. **検収チェックリスト**

---

## 7. 今回作らないもの（★ここを守る。増やしたくなったら次回に回す）

- 募集ページの**自動取得（スクレイピング）** … 規約違反リスク。手入力でいく
- グラフ・統計ダッシュボード
- 提案文のAI生成
- チーム共有・複数人での同時編集
- 通知メール（パスワード再設定を除く）
- モバイルアプリ・PWA
- 決済・課金
- ダークモード切替
- 画像のアップロード
- 多言語対応

---

## 8. 竹元さんがやる必要があること（Claudeは代行できない）

**アカウント作成とパスワード入力は本人しかできません。**

1. **Supabase** のアカウント作成（GitHubログインでOK・無料枠） https://supabase.com
2. **Vercel** のアカウント作成（GitHubログインでOK・無料枠） https://vercel.com
3. GitHubに `apply-tracker` リポジトリを作る（`gh repo create` で可・Claudeが手伝える）

※ 1と2が済んだら、接続キー（URLとanonキー）を `.env.local` に貼る作業が発生します。
   **キーそのものはGitに入れません。** `.env.example` には名前だけ書きます。

---

## 9. 注意（前回までに踏んだ地雷）

- `confirm()` / `alert()` は使わない。**確認は画面内のUIで作る**
- 色は目視ではなく**コントラスト比を数値で測る**（4.5以上）
- Codexは2分を超えることがある。**バックグラウンドで実行する**
- GitHub Actions の書き込み権限は初期値が `read`。自動コミットさせるなら要変更
