# ApplyTracker

クラウドソーシング案件への応募状況を記録する Next.js + Supabase アプリです。現在は登録、ログイン、ログアウト、セッション保護とRLS統合テストまで実装しています。

## 必要な環境

- Node.js 20以上、npm 10以上
- Supabaseプロジェクト

## セットアップ

リポジトリを取得したら、プロジェクト直下で依存関係をインストールします。

```bash
npm install
copy .env.example .env.local
```

macOS / Linuxでは2行目を `cp .env.example .env.local` にしてください。

### 1. migrationを適用する

Supabaseでプロジェクトを作成し、ダッシュボードの **SQL Editor** を開きます。`supabase/migrations/` 内のSQLファイルをファイル名順に開き、内容をSQL Editorへ貼り付けて **Run** を押します。現在は次の1ファイルです。

```text
supabase/migrations/202608290001_initial_schema.sql
```

SQLが正常終了したことと、Table Editorに `profiles`、`applications`、`audit_logs` が作成されたことを確認してください。同じmigrationを同じDBへ二度実行すると型やテーブルの重複エラーになるため、一度だけ実行します。

### 2. 環境変数を設定する

Supabaseダッシュボードの **Project Settings > API**（表示名が異なる場合はConnect画面）で値を確認し、`.env.local` に設定します。

```dotenv
NEXT_PUBLIC_SUPABASE_URL=プロジェクトURL
NEXT_PUBLIC_SUPABASE_ANON_KEY=公開用anonキー
SUPABASE_SERVICE_ROLE_KEY=サーバー専用service_roleキー
```

`SUPABASE_SERVICE_ROLE_KEY` はRLS統合テストのユーザー作成と後片付けだけに使います。ブラウザへ渡したり、Gitへコミットしたりしないでください。`.env.local` はGit管理対象外です。

### 3. 起動する

```bash
npm run dev
```

ブラウザで `http://localhost:3000/login` を開きます。新規登録後、SupabaseのAuth設定でメール確認が有効なら、届いた確認メールのリンクを開いてからログインします。ログイン後は `/applications` に移動し、メールアドレスとログアウトボタンが表示されます。

## テスト

```bash
npm test
```

RLS統合テストは、確認済みのユーザーA/Bを一時作成し、Aの案件をBが参照・更新・削除できないことと、A自身は参照できることを検証します。終了時は成功・失敗を問わず、作成した行とユーザーを削除します。必要な環境変数が未設定なら、テスト結果には失敗ではなくスキップと表示されます。

品質チェックは次のコマンドで実行します。

```bash
npm run lint
npm run build
```

## 障害復旧

- 環境変数エラー: `.env.local` の3項目を確認し、開発サーバーを再起動します。
- DBエラー: migrationが対象プロジェクトに適用済みか確認します。既存DBを直接変更せず、新しいmigrationを追加してください。
- 認証エラー: SupabaseダッシュボードのAuthentication設定とユーザー一覧を確認します。
- 秘密値を公開した場合: Git履歴から除くだけでなく、Supabase側で直ちにキーを再発行してください。

## 現在の制約

案件CRUD画面、パスワード再設定、管理者画面、Playwright E2E、CI、Vercel公開は今後の周で実装します。スクレイピング、統計、AI生成、チーム共有、通知、PWA、決済、ダークモード、画像、多言語対応は仕様上の対象外です。

## 検証記録（1周目・2026-08-29）

Claudeがブラウザと実DBで確認した結果。

| 確認項目 | 結果 |
|---|---|
| RLS統合テスト（ユーザーA/B） | ✅ 通過（`npm test`） |
| 未ログインで `/applications` にアクセス | ✅ `/login` へ誘導 |
| ログイン → `/applications` | ✅ メールアドレスが表示される |
| ログアウト → 再アクセス | ✅ 再び遮断される |
| 不正なメールでの登録 | ✅ 画面内に日本語エラー（`alert()` 不使用） |
| 新規ユーザーの `profiles` 自動作成 | ✅ `role=user` で作られる |
| コントラスト比 11箇所 | ✅ 全て基準以上（最低 6.10 / 基準 4.5） |
| 検証後のテストデータ | ✅ 全削除（ユーザー0・案件0・ログ0） |

### 未確認・次周に持ち越し

- **新規登録の成功パターンは未確認。** Supabaseの初期設定ではメール確認が必須のため、
  実際の登録には確認メールのリンクを開く操作が要る可能性がある。
  Supabase の Authentication → Sign In / Providers → Email の
  「Confirm email」で挙動を確認・変更できる
- 管理者ロールの権限ポリシー（4周目）
- パスワード再設定（4周目）

## 検証記録（2周目・2026-08-29）

Claudeがブラウザと実DBで、実在の募集データを使って確認した結果。

| 確認項目 | 結果 |
|---|---|
| ビルド / 型 / lint / テスト10件 | ✅ 全通過 |
| 1件も無いときの案内 | ✅ 表示される |
| 案件の登録 → 一覧に反映 | ✅ 媒体・状態は日本語、金額は 50,000円 と整形 |
| 編集（既存値9項目の読み込み） | ✅ 全項目が正しく入る |
| 編集の保存 | ✅ 50,000円→75,000円 / 検討中→応募済 |
| **サーバー側の入力チェック** | ✅ 画面側の制限を外して評価9を送っても弾き、項目の横に日本語で理由を表示 |
| **削除の確認UI** | ✅ 媒体・タイトル・締切・状態を表示（前回のバグは再発なし） |
| 削除の取り消し | ✅ 何も消えない |
| 削除の実行 | ✅ 対象の1件だけ消える |
| `confirm()` / `alert()` | ✅ 不使用 |
| **画面幅375pxでの表** | ✅ 表790pxを入れ物(323px)に閉じ込め、ページ自体は横スクロールしない |
| **コントラスト比 37箇所** | ✅ 全て基準以上（最低 7.86） |
| 検証後のテストデータ | ✅ 全削除（ユーザー0・案件0・プロフィール0・ログ0） |

### 検証中に分かったこと（実装の問題ではない）

- ブラウザ自動操作で `requestSubmit()` を**ボタンを介さず**呼ぶと、入力チェックで弾かれた際に
  エラー表示に至らないことがある。**実際のクリック操作では正常**であることを確認済み。
  検証時は必ず送信ボタンを指定すること

## 検証記録（本人の指摘による手直し・2026-08-30）

| 確認項目 | 結果 |
|---|---|
| ビルド / 型 / lint / テスト13件 | OK 全通過 |
| migration適用後の列 | OK 新4列あり・旧listed_amount_textは削除済み |
| **DB制約（プログラムを通さず直接投入）** | OK 上限<下限・小区分だけ・下限なし上限のみ、3種とも拒否 |
| ジャンル大区分→小区分の絞り込み | OK 業務自動化は4件、インフラは3件に切り替わる |
| 「幅あり」チェック | OK オフで上限欄が消え、オンで現れる |
| カンマ表示（入力欄の下） | OK 50,000円 / 100,000円 / 75,000円 |
| 一覧の金額表示 | OK `50,000円 〜 100,000円`、幅なしは `50,000円` |
| 一覧のジャンル列 | OK `業務自動化・ツール / データ取得・スクレイピング` |
| 編集画面の値の復元 | OK ジャンル2段・金額・「幅あり」の状態まで復元 |
| 上限<下限を画面から送信 | OK 保存されず「上限は下限以上の金額を入力してください。」 |
| 「幅あり」オフで保存 | OK 上限がクリアされ一覧が単一表示になる |
| 画面幅375px・8列 | OK 表1040pxを入れ物(323px)に閉じ込め、ページは横に伸びない |
| コントラスト比 22箇所 | OK 全て基準以上（最低 7.86） |
| 検証データの後始末 | OK 検証用ユーザーのみ削除（本人アカウントは保持） |

### 注意

- 手直し前に登録した案件がある場合、**「一覧に出ていた金額」は作り替えたため空になっている。**
  数値で入れ直すこと（他の項目はそのまま残っている）

## 検証記録（共通ヘッダー・未保存離脱の確認・2026-08-30）

| 確認項目 | 結果 |
|---|---|
| ビルド / 型 / lint / テスト17件 | OK 全通過 |
| ヘッダーが全ページに（ログイン画面含む） | OK 見出しの重複なし |
| ログアウト中に「ApplyTracker」 | OK ログイン画面へ |
| ログイン中に「ApplyTracker」 | OK 案件一覧へ |
| 何も変えずに「取り消す」 | OK 確認は出ない |
| 値を変えて「取り消す」 | OK 「このページを離れますか？変更は保存されません」 |
| 「編集を続ける」 / Escapeキー | OK 留まる・入力も残る |
| 「離れる」 | OK 出ていき、変更は保存されていない |
| 新規登録ページにも同じ仕組み | OK |
| 保存成功時 | OK 邪魔されずに保存できる |
| コントラスト比 33箇所＋モーダル内3箇所 | OK 全て基準以上（最低 7.86） |
| 画面幅375px | OK ページもモーダルもはみ出さない |

### この工程で見つけて直したバグ

**未保存の状態で画面最上部のヘッダー「ApplyTracker」を押すと、確認パネルが見えなかった。**

- 実測: パネルの上端 1568px / 画面の高さ 900px / スクロール位置 0 → 完全に画面外
- 利用者からは「押しても何も起きない」ように見える（前回の「削除ボタンが動かない」と同型）
- 原因: 確認パネルをフォームの末尾に描画していた（position: static）
- 対処: position: fixed の中央モーダルに変更。オーバーレイ・背景スクロール抑止・
  Escape対応・開いたら「編集を続ける」にフォーカス
- 修正後の実測: 1280x900で上端410px、375x812で上端355px・左34px・右338px。いずれも画面内

## 検証記録（3周目・絞り込みとCSV出力・2026-08-30）

危険な値（数式・カンマ・改行・引用符）を含むデータ4件を用意して確認した。

### 絞り込み

| 確認項目 | 結果 |
|---|---|
| ビルド / 型 / lint / テスト24件 | OK 全通過 |
| 状態 / 媒体 / ジャンル大区分 の3つ | OK いずれも正しく絞り込む |
| 複数条件の同時指定 | OK |
| URLに条件が残る | OK `?status=applied` の形 |
| **不正な値（スクリプトタグ含む）** | OK 無視して全件表示。落ちない |
| 件数表示 | OK `1件 / 全4件` |
| 0件のとき | OK 案内文と解除の導線あり |
| 絞り込みを解除 | OK 全件に戻る |

### CSV出力

| 確認項目 | 結果 |
|---|---|
| 先頭3バイト | OK `EF BB BF`（BOMあり。Excelで文字化けしない） |
| Content-Type | OK `text/csv; charset=utf-8` |
| ファイル名 | OK `案件応募_2026-08-30.csv`（filename* でUTF-8指定） |
| カンマを含む値 | OK `"カンマ, を含む案件"` |
| 改行・引用符を含む値 | OK 引用符で囲み、内側の `"` は `""` に |
| **数式注入 `=1+1`** | OK `'=1+1` に無害化 |
| **数式注入 `@SUM(1,2)`** | OK `"'@SUM(1,2)"` に無害化 |
| **数式注入 `-2+3`** | OK `'-2+3` に無害化 |
| 状態・媒体・ジャンル | OK 日本語ラベルで出力 |
| 金額 | OK カンマなしの生の数値（表計算で計算できる） |
| 絞り込みの引き継ぎ | OK CSVボタンが現在の条件を持つ |
| 0件のとき | OK 見出し行だけのファイル |
| **他人のデータの混入** | OK DB全体6件中、自分の4件のみ出力（RLSが効いている） |
| コントラスト比 58箇所 | OK 全て基準以上（最低 7.28） |
| 画面幅375px | OK 絞り込み欄が縦並びになり、はみ出さない |
| 検証データの後始末 | OK 検証用ユーザーのみ削除 |

### 残っている軽微な点（機能に影響なし）

- 絞り込みフォームを送信すると URL に空の項目が残る（`?status=applied&platform=&genre=`）。
  読めなくはないが、空の項目は落とした方がきれい。CSVボタンのリンクは既に落としている

## 検証記録（一覧表の折り返し修正・2026-08-30）

本人の指摘「ジャンルが折り返して不格好」への対応。

| 確認項目 | 結果 |
|---|---|
| ジャンルを大区分・小区分の2行に | OK 小区分は小さめ（14px・コントラスト比10.17） |
| 小区分なしの案件 | OK 大区分だけ1行 |
| ジャンルなしの案件 | OK `—` |
| **他の列の途中折り返し** | OK 解消（「クラウドワー/クス」「応募/済」「2026-09-/08」が1行に） |
| タイトル列 | OK 長い場合は折り返す（min-width 9em） |
| **操作列（編集・削除）** | OK 右端に固定。横スクロールしても常に押せる |
| 画面幅375px | OK ページははみ出さず、操作列も見える |
| コントラスト比 50箇所 | OK 全て基準以上（最低 7.28） |
| ビルド / lint / テスト24件 | OK 全通過 |

### 副作用と対処

- 途中折り返しを止めた結果、表の幅が 1040px → 1117px に広がり、
  **「削除」ボタンが見切れる**状態になった（入れ物は1060px）
- 対処: 操作列を `position: sticky; right: 0` で右端に固定。
  横スクロールしても操作ボタンが常に見える形にした。狭い画面でも同じ
