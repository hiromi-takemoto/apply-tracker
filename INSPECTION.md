# 納品前検品報告

検品日: 2026-08-31  
立場: 発注者側（README・既存検証記録は自己申告として扱い、実物を再確認）

## 1. 検収の可否

**条件付き合格（現状のまま最終受領はしない）**。

主要な静的検査、型検査、ビルド、Vitest 31件は通過し、migration最終形とER図、直接依存の版は概ね一致した。一方、納品書類に事実誤認（ライセンス）、古い記述（公開状況）、環境変数の説明漏れがあり、正式な受入テスト結果も空欄である。また本番の認証保護と配信JSの秘密値不在は、今回の実行環境のTLSエラーにより独立再検証できていない。下記必須事項の是正・再確認を受領条件とする。

## 2. 必ず直すべき問題

1. **ライセンス一覧が不正確**: `docs/ライセンス.md` は「GPLのようなものは含まれない」と断定するが、インストール済み間接依存 `@img/sharp-wasm32` / `@img/sharp-win32-x64` は LGPL-3.0-or-later を含む。直接依存だけの表で全体の義務不在を断定しており、完全な依存・ライセンス・NOTICE要否の棚卸しになっていない。
2. **READMEの環境変数一覧が不足**: `.env.example` と `src/app/reset-password/actions.ts` は `NEXT_PUBLIC_SITE_URL` を使用するが、READMEの設定例・GitHub Secrets説明は3変数のみ。README自身の後半では「4つ揃っている」と主張しており自己矛盾している。
3. **READMEが現状と矛盾**: 冒頭・6周目では本番公開済みだが、「現在の制約」に「Vercel公開は今後の周で実装」と残っている。納品時点の手順書として誤誘導する。
4. **受入テスト結果が未納**: `docs/受入テスト表.md` は53項目とも「実際の結果」が空欄。READMEの制作者側検証記録はあるが、対応する53項目の実施日・環境・結果・証跡を備えた受入結果ではない。
5. **本番セキュリティ再検証を完了すること**: `/applications`、`/admin`、`/applications/export` の未ログイン保護、および本番配信JSの `sb_secret_` 実値不在を、発注者が実行できるcurl/走査手順と結果で再提示する。READMEの自己申告だけでは検収根拠にしない。

## 3. 直した方がよい点（重要度順）

1. `npm ci`、E2E、本番curlを含む再現可能な検収スクリプトと、CI実行URL・コミットSHA・成果物を残す。現在の文書は結果の転記が中心で追跡性が弱い。
2. `npm test` は環境変数不足時にRLS統合テストをskipできるため、「31件通過」と「実DBでRLS確認済み」を分離表示する。今回の実行では31件通過したが、一般の再実行で同じ保証にはならない。
3. 引き渡し手順は手順書のみで、GitHub/Supabase/Vercelの所有権移管済み証跡、制作者権限削除日、鍵ローテーション完了欄がない。チェック欄と証跡リンクを追加する。
4. 運用設計として、復旧目標、バックアップ取得・復元テスト、監視/アラート、障害連絡先、ログ保管期間、個人情報の保存・削除方針、脆弱性対応/SLAがない。
5. 対応ブラウザにSafariを掲げる受入表に対し、既知の制約ではSafari/Firefox未確認。対応保証範囲を明確化する。
6. READMEは検証日誌が長く、現在有効な手順と過去の未確認事項が混在する。納品版の現行情報と開発履歴を分離する。

## 4. 納品10点セット判定

| # | 納品物 | 判定 | 根拠 |
|---|---|:---:|---|
| 1 | ソース一式 | ○ | Next.jsソース、lockfile、設定、単体/E2E、CIが存在。型・build・31テストを実行できた。 |
| 2 | 本番URL | △ | URLはREADMEに記載。ただし今回curlはローカルTLS資格情報エラーで到達確認不能。 |
| 3 | DBスキーマとmigration | ○ | SQLは3本。2本目で `listed_amount_text` を削除し金額4列・ジャンル2列を追加、3本目で管理集計関数を追加。ERは最終テーブル列と一致。 |
| 4 | 環境変数一覧 | △ | `.env.example` は4変数だが、READMEの設定例は `NEXT_PUBLIC_SITE_URL` を欠く。 |
| 5 | README | △ | セットアップ等は充実するが、公開済み/今後公開、3変数/4変数の矛盾あり。 |
| 6 | 受入テスト表と結果 | × | 53項目と実画面の主要導線は対応するが、結果欄が全て空。E2E再実行も `spawn EPERM` で未完。 |
| 7 | 引き渡し手順 | △ | 3サービスの手順は実用的だが、移管完了証跡・鍵更新完了記録がない。 |
| 8 | 既知の制約 | ○ | 並び順、CSV取込なし、共有なし等をルート・実装検索で確認。未検証ブラウザも明記。運用条件の正確性は契約/各サービス画面で別確認が必要。 |
| 9 | ライセンス一覧 | × | 直接依存の版・ライセンスはpackage.json/node_modulesと一致するが、間接LGPL依存を落としたままGPL系不在と断定。 |
| 10 | 検収チェックリスト | △ | 文書は存在し観点もあるが、済欄が空で、権限移管・CI緑などの証跡がない。 |

## 5. 実行したコマンドと結果

| コマンド | 結果 |
|---|---|
| `npm ci` | **失敗**（exit 1、依存再構築時 `spawn EPERM`）。環境要因の可能性あり。 |
| `npm ci --ignore-scripts --cache .npm-cache` | 成功、395 packages、脆弱性0。標準 `npm ci` の代替であり同等とは扱わない。 |
| `npm run lint` | 成功（exit 0）。 |
| `npx tsc --noEmit` | 成功（exit 0）。 |
| `npm run build` | 成功（exit 0、Next.js 16.3.3、全12 route生成）。先頭の独立tscも成功。 |
| `npm test` | 成功（7 files、31/31 tests）。 |
| `npm run test:e2e` | **失敗**（Playwright起動時 `spawn EPERM`、テスト未実行）。 |
| `curl.exe -sS ...`（本番4 URL） | **接続前に失敗**（Schannel `SEC_E_NO_CREDENTIALS`）。アプリ障害とは判定不能。 |
| `rg` / `git ls-files` / `git log -S` | `.env.local` は未追跡。追跡中ソースに秘密実値なし。`sb_secret_` 文字列の履歴ヒットはREADMEの検査語のみ。 |
| `.next/static` 走査 | `sb_secret_` はライブラリ内の接頭辞文字列（長さ10）が1件。`.env.local` のservice-role実値との完全一致は0件。これはローカルbuildの確認で、本番配信物の証明ではない。 |
| node_modules license走査 | 直接依存表は一致。間接依存2件で LGPL-3.0-or-later を検出。 |

## 6. 検品しきれなかった範囲

- 本番URLへの到達、リダイレクト/401、配信JS実体の秘密値走査（TLS資格情報エラーのため）。
- ログイン、新規登録メール、パスワード再設定メール、CRUD、CSVのExcel表示、管理者画面、スマホ/Safari/Firefoxの実ブラウザ操作。
- E2E 6本（プロセス起動権限エラー）。README掲載の過去結果は独立証拠として採用していない。
- 実Supabaseに3 migrationが適用済みか、RLS/集計関数が本番DBで現SQLどおりか。今回はSQLとテストコードの静的突合まで。
- GitHub Actionsの実行履歴、Vercel/Supabase/GitHubの所有権移管、鍵ローテーション、デモアカウント7件の実在性・残存状態。
- バックアップ、無料枠、商用利用条件、メールレート制限など外部サービス側の最新契約・設定。

---

# 検品指摘への対応（2026-08-31・制作側記入）

検品で挙がった「必ず直すべき問題」5件への対応。

| # | 指摘 | 裏取り | 対応 |
|---|---|---|---|
| 1 | ライセンス一覧が不正確（間接依存にLGPL） | **指摘どおり。** `@img/sharp-wasm32` と `@img/sharp-win32-x64` が `LGPL-3.0-or-later` を含むことを実測で確認 | `docs/ライセンス.md` を**間接依存388パッケージの集計に差し替え**。LGPL2件を明示し、「GPL系は含まれない」という誤った断定を削除。sharpの出どころと注意点、法務確認の必要性を追記 |
| 2 | READMEの環境変数が3件（実際は4件） | **指摘どおり。** `.env.example` は4件 | READMEの設定例に `NEXT_PUBLIC_SITE_URL` を追加し、任意である旨と既定の挙動を明記。「3項目」の記述も修正 |
| 3 | READMEに「Vercel公開は今後の周で実装」が残存 | **指摘どおり。** 163行目に残っていた | `docs/既知の制約.md` へ誘導する記述に差し替え |
| 4 | 受入テスト結果が全項目空欄 | **指摘どおり** | `docs/受入テスト表.md` の冒頭に**制作者側の実施結果**を追加。実施済/未実施を区分し、主な計測値を記載。**未実施の項目に○は付けていない**（1-2 / 1-3 / 2-4〜2-7 / 5-8 / 5-9 は発注者側で実施） |
| 5 | 本番セキュリティを発注者が再検証できる手順がない | **指摘どおり** | `docs/検証手順.md` を新規作成。保護の確認・CSVの直接取得・配信JSの秘密値走査・RLS・CIの確認を、貼り付けて実行できるコマンドで提供 |

## 検品側の環境で失敗した項目の再確認

| 項目 | 検品側の結果 | 制作側で再確認した結果 |
|---|---|---|
| `npm ci` | 失敗（`spawn EPERM`） | **Windows実機で成功（終了コード0・脆弱性0）。** CI（Ubuntu）でも成功。検品環境の権限による failure と判断 |
| `npm run test:e2e` | 失敗（`spawn EPERM`） | 手元で6本すべて成功（54.6秒）。CIでも success |
| 本番URLへのcurl | 失敗（Schannel `SEC_E_NO_CREDENTIALS`） | 手元で全パスの応答を取得済み。手順を `docs/検証手順.md` に公開し、発注者が再現できる形にした |

## 対応しなかった指摘と理由

「直した方がよい点」のうち、以下は**今回の納品範囲外**として見送った。

| 指摘 | 理由 |
|---|---|
| 復旧目標・バックアップ・監視・SLA・個人情報の保存/削除方針 | **運用保守契約の範囲。** 本納品は開発のみで、保守契約は未締結。契約時に別途定める |
| 所有権移管の完了証跡・鍵ローテーション完了欄 | 移管は**引き渡し実施時**に記録する。手順は `docs/引き渡し手順.md` に記載済み |
| Safari / Firefox の動作保証 | `docs/既知の制約.md` に未確認と明記済み。保証範囲に含めるなら追加費用が必要 |
| READMEの検証日誌と現行手順の分離 | 現時点では開発の経緯を残す価値が上回ると判断。冒頭に現行情報を集約済み |
